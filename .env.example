# Copie para .env: cp .env.example .env
# Paths do mock MSW — não há API externa nesta entrega.
# Produção (Vercel): os mesmos valores estão em .env.production (commitado).
VITE_ENABLE_MSW=true
VITE_API_BASE_URL=/api
VITE_WS_URL=/
